class Solution {
public:
    bool uniformArray(vector<int>& nums1) {
        int n = nums1.size();
        return true;
        //vector<int> nums2(n);
        // odd
        // int odd = 0, even = 0;
        // for(int i = 0; i < n; i++){
        //     if(nums1[i] % 2 != 0) odd++;
        //     else even++;
        // }
        // if(odd == n || even == n) return true;

    }
};